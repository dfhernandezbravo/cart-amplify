import React from 'react';
import { SkeletonContainer, Skeleton as SkeletonElement } from './styles';

const Skeleton = () => {
  return (
    <SkeletonContainer>
      <SkeletonElement />
    </SkeletonContainer>
  );
};
export default Skeleton;
