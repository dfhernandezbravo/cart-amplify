import React from 'react';
import { Container } from './styles';

const ProductAvailableTitle = () => {
  return (
    <Container>
      <p className="title">Productos disponibles</p>
    </Container>
  );
};

export default ProductAvailableTitle;
