export interface Observability {
  eventType: string;
  orderId: string;
  totalAmount: number;
  origin: string;
  orderNumber: string;
  datetime: string;
  channel: string;
  paymentMethod: string;
}
